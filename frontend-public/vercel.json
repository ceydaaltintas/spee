{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "/api/proxy?_p=:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
